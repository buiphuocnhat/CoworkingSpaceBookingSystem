<%
=begin%>
 <div class="container-fluid">
  <div class="container-fluid"><%= render partial: "search"%></div>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-5" style="background-color:red;">
        <p>map</p>
      </div>
      <div class="col-md-7">
        <div class="container-fluid"><%= render partial: "thumbnail", object: @spaces %></div>
        <div class="container-fluid"><%= will_paginate @spaces %></div>
      </div>
    </div>
  </div>
</div>
<%
=end%>
<div>
  <div ><%= render partial: "search"%></div>
  <div>
        <div class="container-fluid"><%= render partial: "thumbnail", object: @spaces %></div>
        <div class="container-fluid"><%= will_paginate @arrSpaces %></div>
  </div>
</div>
